{
  "name": "pruebatecnicacypress",
  "version": "1.0.0",
  "description": "Automatizacion del login de acceso de la pagina web Swag Labs",
  "main": "index.js",
  "browsers": [
    "chrome",
    "firefox",
    "edge"
  ],
  "scripts": {
    "test": "cypress open",
    "runChrome": "cypress run",
    "cy:run": "cypress run --headed",
    "cy:parallel1": "cypress-parallel -s cy:run -t 3 -d ./cypress/e2e/",
    "cy:run:chrome": "cypress run --headed --browser chrome",
    "cy:run:firefox": "cypress run --headed --browser firefox",
    "cy:run:edge": "cypress run --headed --browser edge",
    "cy:parallel2": "npm run cy:run:chrome && npm run cy:run:firefox && npm run cy:run:edge"
  },
  "author": "Renato Cabezas",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.3.1",
    "cypress-parallel": "^0.13.0"
  }
}
